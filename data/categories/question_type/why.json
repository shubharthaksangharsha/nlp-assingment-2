{
  "category": "why",
  "post_count": 1,
  "posts": [
    {
      "title": "CBOW v.s. skip-gram: why invert context and target words?",
      "description": "<p>In <a href=\"https://www.tensorflow.org/versions/r0.9/tutorials/word2vec/index.html#vector-representations-of-words\" rel=\"noreferrer\">this</a> page, it is said that: </p>\n\n<blockquote>\n  <p>[...] skip-gram inverts contexts and targets, and tries to predict each context word from its target word [...]</p>\n</blockquote>\n\n<p>However, looking at the training dataset it produces, the content of the X and Y pair seems to be interexchangeable, as those two pairs of (X, Y): </p>\n\n<blockquote>\n  <p><code>(quick, brown), (brown, quick)</code></p>\n</blockquote>\n\n<p>So, why distinguish that much between context and targets if it is the same thing in the end? </p>\n\n<p>Also, doing <a href=\"https://github.com/tensorflow/tensorflow/blob/master/tensorflow/examples/udacity/5_word2vec.ipynb\" rel=\"noreferrer\">Udacity's Deep Learning course exercise on word2vec</a>, I wonder why they seem to do the difference between those two approaches that much in this problem: </p>\n\n<blockquote>\n  <p>An alternative to skip-gram is another Word2Vec model called CBOW (Continuous Bag of Words). In the CBOW model, instead of predicting a context word from a word vector, you predict a word from the sum of all the word vectors in its context. Implement and evaluate a CBOW model trained on the text8 dataset.</p>\n</blockquote>\n\n<p>Would not this yields the same results?</p>\n",
      "tags": "['nlp', 'tensorflow', 'deep-learning', 'word2vec', 'word-embedding']",
      "accepted_answer": "<p>Here is my oversimplified and rather naive understanding of the difference:</p>\n<p>As we know, <strong>CBOW</strong> is learning to predict the word by the context. Or maximize the probability of the target word by looking at the context. And this happens to be a problem for rare words. For example, given the context <code>yesterday was a really [...] day</code> CBOW model will tell you that most probably the word is <code>beautiful</code> or <code>nice</code>. Words like <code>delightful</code> will get much less attention of the model, because it is designed to predict the most probable word. This word will be smoothed over a lot of examples with more frequent words.</p>\n<p>On the other hand, the <strong>skip-gram</strong> model is designed to predict the context. Given the word <code>delightful</code> it must understand it and tell us that there is a huge probability that the context is <code>yesterday was really [...] day</code>, or some other relevant context. With <strong>skip-gram</strong> the word <code>delightful</code> will not try to compete with the word <code>beautiful</code> but instead, <code>delightful+context</code> pairs will be treated as new observations.</p>\n<p><strong>UPDATE</strong></p>\n<p>Thanks to @0xF for sharing <a href=\"https://www.quora.com/What-are-the-continuous-bag-of-words-and-skip-gram-architectures\" rel=\"noreferrer\">this article</a></p>\n<blockquote>\n<p>According to Mikolov</p>\n<p><strong>Skip-gram:</strong> works well with small amount of the training data, represents well even rare words or phrases.</p>\n<p><strong>CBOW:</strong> several times faster to train than the skip-gram, slightly better accuracy for the frequent words</p>\n</blockquote>\n<p>One more addition to the subject is found <a href=\"https://groups.google.com/d/msg/word2vec-toolkit/LNPeC5gyhmQ/p8683JkD6LoJ\" rel=\"noreferrer\">here</a>:</p>\n<blockquote>\n<p>In the &quot;skip-gram&quot; mode alternative to &quot;CBOW&quot;, rather than averaging\nthe context words, each is used as a pairwise training example. That\nis, in place of one CBOW example such as [predict 'ate' from\naverage('The', 'cat', 'the', 'mouse')], the network is presented with\nfour skip-gram examples [predict 'ate' from 'The'], [predict 'ate'\nfrom 'cat'], [predict 'ate' from 'the'], [predict 'ate' from 'mouse'].\n(The same random window-reduction occurs, so half the time that would\njust be two examples, of the nearest words.)</p>\n</blockquote>\n",
      "other_answers": "['<p>It has to do with what exactly you\\'re calculating at any given point.  The difference will become clearer if you start to look at models that incorporate a larger context for each probability calculation.</p>\\n\\n<p>In skip-gram, you\\'re <strong>calculating the context word(s)</strong> from the word at the current position in the sentence; you\\'re \"skipping\" the current word (and potentially a bit of the context) in your calculation.  The result can be more than one word (but not if your context window is just one word long).</p>\\n\\n<p>In CBOW, you\\'re <strong>calculating the current word</strong> from the context word(s), so you will only ever have one word as a result.  </p>\\n', '<p>In Deep Learning Course, from coursera <a href=\"https://www.coursera.org/learn/nlp-sequence-models?specialization=deep-learning\" rel=\"nofollow noreferrer\">https://www.coursera.org/learn/nlp-sequence-models?specialization=deep-learning</a> you can see that Andrew NG doesn\\'t switch the context-target concepts. It means that target word will ALWAYS be treated as the word to be predicted, no matter if is CBOW or skip-gram.</p>\\n']",
      "processed_title": "cbow vs skipgram why invert context and target words",
      "processed_description": "page said skipgram inverts contexts targets tries predict context word target word however looking training dataset produces content x pair seems interexchangeable two pairs x quick brown brown quick distinguish much context targets thing end also udacitys deep learning course exercise word2vec wonder seem difference two approaches much problem alternative skipgram another word2vec model called cbow continuous bag words cbow model instead predicting context word word vector predict word sum word vectors context implement evaluate cbow model trained text8 dataset would yields results",
      "processed_accepted_answer": "oversimplified rather naive understanding difference know cbow learning predict word context maximize probability target word looking context happens problem rare words example given context yesterday really day cbow model tell probably word beautiful nice words like delightful get much less attention model designed predict probable word word smoothed lot examples frequent words hand skipgram model designed predict context given word delightful must understand tell us huge probability context yesterday really day relevant context skipgram word delightful try compete word beautiful instead delightfulcontext pairs treated new observations update thanks 0xf sharing article according mikolov skipgram works well small amount training data represents well even rare words phrases cbow several times faster train skipgram slightly better accuracy frequent words one addition subject found skipgram mode alternative cbow rather averaging context words used pairwise training example place one cbow example predict ate averagethe cat mouse network presented four skipgram examples predict ate predict ate cat predict ate predict ate mouse random windowreduction occurs half time would two examples nearest words",
      "processed_other_answers": "[]",
      "processed_tags": "[]"
    }
  ]
}